import 'package:flutter/material.dart';

class AddQuizDialog extends StatelessWidget {
  const AddQuizDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dialog(
      child: Container(
        height: 500,
        width: double.infinity,
        child: Column(),
      ),
    );
  }
}
